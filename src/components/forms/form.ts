import { Block } from "../../utils/block";


export class Form extends Block {
    constructor(propsAndChildren) {
        // Создаём враппер дом-элемент button
    super("div", propsAndChildren);
    inputs = propsAndChildren.///потом

  }

  render() {

    return this.forming;
  }
}
